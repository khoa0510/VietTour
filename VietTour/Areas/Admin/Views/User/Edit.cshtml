@{
    ViewData["Title"] = "Index";
    //Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}
                                                    <!--EDIT-USER-->
@model EditUserViewModel
<div class="card card-primary">
    <!-- /.card-header -->
    <!-- form start -->

    <form method="post">
        <div class="card-body">
            
            <div class="form-group">
                <label for="Email">Email</label>
                <input type="email" class="form-control" id="Email" name="Email" @*value="@Model.Email"*@ placeholder="Nhập email">
            </div>

            <div class="form-group">
                <label for="Phone">Số điện thoại</label>
                <input type="number" class="form-control" id="Phone" name="Phone" @*value="@Model.Phone"*@ placeholder="Nhập số điện thoại">
            </div>

            <div class="form-group">
                <label for="Username">Tên người dùng</label>
                <input type="text" class="form-control" id="Username" name="Username" @*value="@Model.Username"*@ placeholder="Nhập tên người dùng">
            </div>
            <div class="form-group">
                <label for="Address">Địa chỉ</label>
                <input type="text" class="form-control" id="Address" name="Address" @*value="@Model.Address"*@ placeholder="Nhập địa chỉ">
            </div>

        </div>

        <!-- /.card-body -->
        @*Lưu*@
        <div class="card-footer">
            <button type="submit" class="btn btn-primary">Lưu</button>
        </div>
    </form>
</div>

